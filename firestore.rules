
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For now, allow read/write to all documents to support guest access.
    // This will be replaced with authenticated rules later.
    match /results/{docId} {
      allow read, write: if true;
    }
  }
}
